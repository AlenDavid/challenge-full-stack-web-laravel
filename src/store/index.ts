import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

// eslint-disable-next-line
const store: any = new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {},
});

export default store;
